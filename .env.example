# ==============================================================================
# YOLOv10 Person Detection - Configuration Template
# ==============================================================================
# Copy this file to .env and configure your settings
# cp .env.example .env

# ------------------------------------------------------------------------------
# Dataset Configuration
# ------------------------------------------------------------------------------
# Path to COCO dataset directory
COCO_DATASET_PATH=./data/coco

# Path to processed person detection dataset
PERSON_DATASET_PATH=./datasets/coco_person

# Dataset YAML configuration file
DATASET_YAML=dataset.yaml

# ------------------------------------------------------------------------------
# Model Configuration
# ------------------------------------------------------------------------------
# Model size: n (nano), s (small), m (medium), l (large), x (xlarge)
MODEL_SIZE=s

# Use pretrained weights (true/false)
USE_PRETRAINED=true

# ------------------------------------------------------------------------------
# Training Configuration
# ------------------------------------------------------------------------------
# Number of training epochs
EPOCHS=50

# Batch size (adjust based on your GPU memory)
# Recommended: 16-32 for M4 GPU, 32-64 for NVIDIA GPUs
BATCH_SIZE=24

# Input image size
IMAGE_SIZE=640

# Device: mps (Apple Silicon), cuda (NVIDIA), cpu
DEVICE=mps

# Number of dataloader workers (0 for macOS/MPS)
WORKERS=0

# Learning rate
LEARNING_RATE=0.001

# ------------------------------------------------------------------------------
# Output Configuration
# ------------------------------------------------------------------------------
# Project directory for training runs
PROJECT_DIR=runs/train

# Experiment name
EXPERIMENT_NAME=yolov10_person

# Save checkpoint every N epochs
SAVE_PERIOD=10

# Early stopping patience (epochs)
PATIENCE=50

# ------------------------------------------------------------------------------
# Inference Configuration
# ------------------------------------------------------------------------------
# Confidence threshold for detection (0.0 - 1.0)
CONFIDENCE_THRESHOLD=0.5

# IoU threshold for NMS (0.0 - 1.0)
IOU_THRESHOLD=0.45

# Max detections per image
MAX_DETECTIONS=300

# Show inference results (true/false)
SHOW_RESULTS=true

# Save inference results (true/false)
SAVE_RESULTS=true

# Output directory for inference results
INFERENCE_OUTPUT_DIR=runs/detect

# ------------------------------------------------------------------------------
# Video Inference Configuration
# ------------------------------------------------------------------------------
# Video source: 0 (webcam), path to video file, or RTSP stream
VIDEO_SOURCE=0

# Save output video (true/false)
SAVE_VIDEO=true

# Display FPS on video (true/false)
SHOW_FPS=true

# ------------------------------------------------------------------------------
# Advanced Training Configuration
# ------------------------------------------------------------------------------
# Enable mixed precision training (true/false)
# Note: Set to false for Apple Silicon MPS
AMP=false

# Image caching: false, true, or 'ram'
CACHE=false

# Optimizer: AdamW, Adam, SGD
OPTIMIZER=AdamW

# Weight decay
WEIGHT_DECAY=0.0005

# Warmup epochs
WARMUP_EPOCHS=3.0

# Data augmentation settings
HSV_H=0.015
HSV_S=0.7
HSV_V=0.4
DEGREES=0.0
TRANSLATE=0.1
SCALE=0.5
FLIPLR=0.5
MOSAIC=1.0

# ------------------------------------------------------------------------------
# Hardware Configuration
# ------------------------------------------------------------------------------
# GPU memory limit (GB) - leave empty for auto
GPU_MEMORY_LIMIT=

# Enable deterministic training (may reduce speed)
DETERMINISTIC=true

# ------------------------------------------------------------------------------
# Logging Configuration
# ------------------------------------------------------------------------------
# Verbosity level: true (detailed), false (minimal)
VERBOSE=true

# Enable TensorBoard logging (true/false)
TENSORBOARD=false

# Enable Weights & Biases logging (true/false)
WANDB=false

# Weights & Biases project name
WANDB_PROJECT=yolov10-person-detection

# ------------------------------------------------------------------------------
# Notes
# ------------------------------------------------------------------------------
# - For Apple Silicon (M1/M2/M3/M4): Use DEVICE=mps, WORKERS=0, AMP=false
# - For NVIDIA GPUs: Use DEVICE=cuda, adjust WORKERS and BATCH_SIZE
# - For CPU only: Use DEVICE=cpu (training will be very slow)
# - Reduce BATCH_SIZE if you encounter out-of-memory errors
# - Higher EPOCHS = better accuracy but longer training time
# - MODEL_SIZE 's' offers good balance between speed and accuracy
